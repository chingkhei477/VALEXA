/**
 * VALEXA — site configuration
 * Fill in the real business details below. Any field left as an empty
 * string is automatically hidden wherever it would otherwise appear
 * (footer, Contact page), so the site never ships with unfilled text.
 */
const VALEXA_CONFIG = {
  companyName: "Valexa",
  supportEmail: "support@valexa.in",
  phone: "",          // e.g. "+91 90000 00000"
  address: "",         // e.g. "4th Floor, Keibul Complex, Imphal, Manipur, India"
  openingHours: "",    // e.g. "Mon–Sat, 10:00 AM – 6:30 PM IST"
};
